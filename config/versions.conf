# versions.conf
# -------------------------------------------
# Pin component versions for reproducible builds.
# Check https://github.com/comfyanonymous/ComfyUI/releases for updates.

COMFYUI_VERSION="v0.3.47"    # Use a specific tag for stability (see releases)
# COMFYUI_VERSION="master"   # (Advanced) Use for latest features, not recommended for production
# COMFYUI_VERSION="abcdef1234567890abcdef1234567890abcdef12"  # Pin to a commit SHA for full reproducibility

# Tip: Update this value whenever a new stable release is required.
